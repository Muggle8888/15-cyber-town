# F-009 本地遗留资源处置清单

更新时间：2026-09-19

## 当前结论

F-009 已完成开发、验收、Git 交付、PR #13 合并及合并后 CI。正式仓库与 Git 内归档必须保留；一个功能 worktree 和两个仓库外测试证据根已满足“进入清理审计”的条件，但本轮没有授权删除，因此状态统一为 `CONDITIONAL_DELETE / NOT_DELETED`。

原始 SQLite、运行目录和测试现场没有完整进入 Git，删除后不能仅凭仓库恢复。本清单只确定保留边界和删除前置条件，不是删除授权或删除命令。

## 必须保留

| 路径或对象 | 状态 | 理由 |
| --- | --- | --- |
| `E:/Agent/comprehensive-cases/15-cyber-town` | `KEEP` | 正式项目仓库和唯一权威文档入口 |
| `docs/project-management/` | `KEEP` | 当前状态、路线图、进度、验收结论和证据索引 |
| `docs/archive/task-cards/F-009-safety-cost-performance.md` | `KEEP` | 已关闭任务卡的完整历史 |
| `docs/archive/task-cards/F-009-implementation-plan.md` | `KEEP` | 已关闭实施计划的完整历史 |
| `docs/archive/F-009-过程记录-20260905/` | `KEEP` | 已进入 Git 的过程记录、索引、manifest 和分片归档 |
| `origin/main@75171492070bddddffef58cc4f0fe9552d40bb77` | `KEEP` | PR #13 合并后的正式交付基线 |

以上内容不得在本地资源清理中删除、移动、改写或用旧 worktree 覆盖。

## 满足条件后可以删除

| 精确路径 | 只读盘点 | 当前状态 | 删除前必须完成 |
| --- | --- | --- | --- |
| `E:/Agent/comprehensive-cases/15-cyber-town-f009` | Git worktree；297文件、78,180,771 bytes；仅`AGENTS.md`有未提交差异 | `CONDITIONAL_DELETE / BLOCKED_BY_DIRTY_FILE` | 保存或明确放弃该差异；复核无其他修改；取得精确删除授权；使用Git worktree机制移除 |
| `E:/Agent/cyber-town-f009-step5-tests` | 13,567文件、5,846目录、1,593,026,019 bytes；无reparse、`.env*`或嵌套Git目录 | `CONDITIONAL_DELETE / NOT_DELETED` | 确认不再需要原始Step 5 SQLite/运行现场；记录最终盘点；取得精确删除授权 |
| `E:/Agent/cyber-town-f009-step6-qa` | 30,565文件、24,376目录、2,887,295,116 bytes；无reparse；含25个env-like名称和90个嵌套`.git`目录 | `CONDITIONAL_DELETE / NOT_DELETED` | 确认不再需要原始Step 6 QA/quality/性能现场；记录最终盘点；取得精确删除授权；整根处置而非按名称零散清理 |

两个证据根合计4,480,321,135 bytes。Step 6 根内的env-like名称和嵌套Git目录来自测试证据表面，本轮没有读取其内容；它们不能被当成真实环境文件使用，也不能据此执行选择性清理。

## 功能 worktree 的未提交差异

`E:/Agent/comprehensive-cases/15-cyber-town-f009/AGENTS.md` 仅增加F-009 worktree读取入口和“正式文档优先”的局部规则。该规则服务于已经结束的临时 worktree，不建议合并回正式 `main`。

- 当前文件SHA-256：`101E4D31E5ED08815D6BF3395C5608C86E0B2154C6F7A8BEE2BB7D1D32DE2032`
- 当前差异文本SHA-256：`1A92713FB3EE46A58DC1BA10C32C708DCCAF04C56B6707AAD8CA38386E8E17CD`
- 功能分支HEAD `69da08982760cff23736a37cdd25c2c779a9ce36` 已核验为PR最终HEAD `a0911b95b657f3fb557d2a422c15836e14fcb9fc` 的祖先，功能提交已进入最终交付链。

推荐在删除 worktree 的独立授权中记录上述差异后明确放弃；如果用户希望保留差异，应先导出补丁到指定归档位置。未经选择不得强制移除 worktree。

## 不在本清单范围

`E:/Agent/.ruff_cache`、`E:/Agent/.tmp`、`E:/Agent/.codex-temp`、其他项目目录及E盘根目录中的SQLite文件不属于F-009清理范围。不得因名称类似缓存或临时文件而顺带删除。

## 推荐处置顺序

1. 先将本地文档收尾分支安全同步到远端并完成文档交付，或形成等价的可恢复备份。
2. 对三个候选路径重新执行只读最终盘点，确认路径、文件数、字节数、reparse和Git状态没有漂移。
3. 对worktree的`AGENTS.md`差异作“导出保留”或“明确放弃”的单一决定。
4. 在单独的精确授权下移除功能 worktree；不得使用资源管理器直接删除登记中的worktree。
5. 在单独的精确授权下整根删除或冷归档两个证据目录；不得只挑选SQLite、`.env*`或嵌套Git目录零散删除。
6. 操作后复核路径不存在或归档目标完整、正式仓干净、端口无监听，并把本清单状态改为`DELETED`或`COLD_ARCHIVED`。

## 当前授权边界

本轮只允许文档化分类。删除、移动、压缩、导出补丁、移除worktree、删除本地或远程分支、推送及创建PR均未授权。
