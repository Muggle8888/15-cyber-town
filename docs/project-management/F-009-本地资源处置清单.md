# F-009 本地遗留资源处置清单

更新时间：2026-09-21

## 当前结论

F-009 已完成开发、验收、Git 交付、PR #13 合并及合并后 CI。正式仓库与 Git 内归档必须保留。2026-09-21只读复核时，一个功能worktree和两个仓库外测试证据根均已在文件系统中不存在；现有仓库证据不能确定删除时间、执行主体或外部授权链，因此当前状态统一记录为 `OBSERVED_ABSENT`，不伪造为Codex已执行删除。

原始 SQLite、运行目录和测试现场没有完整进入 Git，现在不能仅凭仓库恢复。Git仍保留旧功能worktree的可修剪登记；本清单记录当前事实和剩余元数据风险，不是`git worktree prune/remove`、分支删除或其他写操作的授权。

## 必须保留

| 路径或对象 | 状态 | 理由 |
| --- | --- | --- |
| `E:/Agent/comprehensive-cases/15-cyber-town` | `KEEP` | 正式项目仓库和唯一权威文档入口 |
| `docs/project-management/` | `KEEP` | 当前状态、路线图、进度、验收结论和证据索引 |
| `docs/archive/task-cards/F-009-safety-cost-performance.md` | `KEEP` | 已关闭任务卡的完整历史 |
| `docs/archive/task-cards/F-009-implementation-plan.md` | `KEEP` | 已关闭实施计划的完整历史 |
| `docs/archive/F-009-过程记录-20260905/` | `KEEP` | 已进入 Git 的过程记录、索引、manifest 和分片归档 |
| `origin/main@75171492070bddddffef58cc4f0fe9552d40bb77` | `KEEP` | PR #13 合并后的正式交付基线 |

以上内容不得在本地资源清理中删除、移动、改写或用旧 worktree 覆盖。

## 已观察为不存在的历史本地路径

| 精确路径 | 2026-09-19历史盘点 | 2026-09-21当前状态 | 剩余事项 |
| --- | --- | --- | --- |
| `E:/Agent/comprehensive-cases/15-cyber-town-f009` | Git worktree；297文件、78,180,771 bytes；仅`AGENTS.md`有未提交差异 | `OBSERVED_ABSENT / GIT_METADATA_STALE` | Git仍登记该worktree并报告`prunable`；清理登记或分支须另行授权，不得重建目录以掩盖状态 |
| `E:/Agent/cyber-town-f009-step5-tests` | 13,567文件、5,846目录、1,593,026,019 bytes；无reparse、`.env*`或嵌套Git目录 | `OBSERVED_ABSENT / RAW_EVIDENCE_UNAVAILABLE` | 当前没有可删除目标；原始Step 5 SQLite与运行现场不能仅凭Git恢复 |
| `E:/Agent/cyber-town-f009-step6-qa` | 30,565文件、24,376目录、2,887,295,116 bytes；无reparse；含25个env-like名称和90个嵌套`.git`目录 | `OBSERVED_ABSENT / RAW_EVIDENCE_UNAVAILABLE` | 当前没有可删除目标；原始Step 6 QA/quality/性能现场不能仅凭Git恢复 |

两个证据根在2026-09-19历史盘点时合计4,480,321,135 bytes。该数字不是本轮可确认的回收量；当前路径已经不存在。Step 6 根内曾记录的env-like名称和嵌套Git目录来自测试证据表面，其内容未被本轮读取，也不能被当成真实环境文件或恢复来源。

## 功能 worktree 的未提交差异

历史盘点显示 `E:/Agent/comprehensive-cases/15-cyber-town-f009/AGENTS.md` 仅增加F-009 worktree读取入口和“正式文档优先”的局部规则。该规则服务于已经结束的临时 worktree，不建议合并回正式 `main`。由于worktree路径当前不存在，差异正文不再能从该路径读取；下列SHA仅作为历史指纹保留。

- 当前文件SHA-256：`101E4D31E5ED08815D6BF3395C5608C86E0B2154C6F7A8BEE2BB7D1D32DE2032`
- 当前差异文本SHA-256：`1A92713FB3EE46A58DC1BA10C32C708DCCAF04C56B6707AAD8CA38386E8E17CD`
- 功能分支HEAD `69da08982760cff23736a37cdd25c2c779a9ce36` 已核验为PR最终HEAD `a0911b95b657f3fb557d2a422c15836e14fcb9fc` 的祖先，功能提交已进入最终交付链。

当前无法再从原worktree导出补丁；Git中的功能提交已经进入最终交付链，但这份未提交局部差异只剩摘要与SHA。不得宣称其正文已归档或可从Git恢复。

## 不在本清单范围

`E:/Agent/.ruff_cache`、`E:/Agent/.tmp`、`E:/Agent/.codex-temp`、其他项目目录及E盘根目录中的SQLite文件不属于F-009清理范围。不得因名称类似缓存或临时文件而顺带删除。

## 推荐收尾顺序

1. 保留正式仓、项目管理文档、任务归档、过程记录分片、机器索引、manifest和合并基线。
2. 将本次文档校准作为新的收尾变化审查；未获授权前不提交、推送或创建PR。
3. 如需消除Git残留，仅建立一个精确的元数据清理任务：先只读确认登记仍指向不存在路径，再单独授权`git worktree prune`或其他最小Git机制。
4. 分支删除与worktree元数据清理分开决策；不得顺带删除本地/远程分支、归档或正式仓文件。
5. 两个证据根当前没有可删除目标；后续只能记录备份发现或外部恢复结果，不能补造原始运行证据。

## 当前授权边界

本轮先授权文档偏移修正与收尾，随后单独授权审查上述15个文档变化、提交并仅推送当前 `docs/f-009-post-merge-closeout` 分支。仍未授权删除、移动、压缩、导出补丁、`git worktree prune/remove`、本地或远程分支删除、测试、服务、PR、合并或`main`修改；实际Git交付结果以终端复核为准。
