# F-009 验收契约

更新时间：2026-09-18
适用状态：QA工具、最终quality10与固定性能1+5均通过；Step 6完成，Step 7等待单独授权。

本文件是 F-009 当前有效长规格的唯一集中位置。它不证明验收通过；运行结论只写入 [evidence.md](evidence.md)。

## 1. 目标与范围

Step 6 只执行独立 fake-only QA、故障与对抗回归、资源边界审计、完整工程门禁及一次固定性能复验。恢复链最终完成一次最小observer修复、tool16、R7、第三个且最终的 `native-quality-10` 及一次固定性能1+5。允许修改三个QA文件并同步正式状态文档；不得修改产品代码、scripts/quality.py、SQL、migration、Godot产品代码、依赖、CI、产品runner、skip、安全语义或性能阈值。

S1 活动路径只保留薄 runner、精确根授权、既有 MachineLedger/ResourceGuard/NativeWatcher/子进程与环境恢复的组合，以及有界脱敏报告。历史兼容逻辑保持只读兼容且停止扩展；不得新增通用沙箱、第二个支持模块或第九个文件。

## 2. 数据、环境与安全边界

- 全程离线、provider disabled；禁止读取真实 .env、真实密钥、真实模型或外部服务。
- 禁止访问 F-005 和旧 SQLite；只用 synthetic fixture、隔离数据库与 sidecar。
- 使用既有解释器；不得创建环境或安装依赖。
- TEMP、TMP、ruff、mypy、uv、Godot 数据与缓存绑定获批根，退出恢复。
- 路径须 canonical，完整父链无 reparse，根全新，并核对文件身份、卷与归属。
- Python 创建路径在操作前精确登记。原生动态文件只允许已批准的目录/类别预登记，运行期逐项记录并结束核对。
- 动态类别限于离线 uv 缓存、Godot import/editor/local cache、隔离 synthetic Git 内部文件、已注册 SQLite 及 sidecar、Godot 本地测试 debug keystore。
- 报告只保留错误码、节点、脱敏路径分类、身份和计数，不展开环境映射、秘密正文或完整原始输出。
- 资源全部保留，Codex 不删除；最终报告大小与人工处置建议。

## 3. 资源与次数

唯一恢复根：E:\Agent\cyber-town-f009-step6-qa\recovery-20260905-01。创建前确认不存在和父链安全，并登记固定子路径、用途、类别和保留条件。新增总量不超过 2 GiB。

- S1 初始 I0 不计修复轮；失败后最多两轮“记录失败与可证伪假设→范围内修改→完整固定清单复验”。换目录、换名字或换对话不重置次数；第二轮后仍有 QA 工具缺陷立即停止。
- S1 仅可按顺序使用 s1-qa-stabilization-i0、s1-qa-stabilization-r1、s1-qa-stabilization-r2；每根不超过 64 MiB，未使用的根不得创建或复用。
- 历史 `native-quality-08` 与 `native-quality-09` 均计为已消费。用户于2026-09-18显式修改原停止规则，只新增一个第三个且最终的 `native-quality-10`；它无论成功或失败均不得再开后续完整quality批次。
- 性能仅在语义全通过且原阻塞充分关闭后执行一次统一 warm-up + 5-run。
- 次数按本轮累计，失败或换目录不能重置。
- 旧诊断及 quality 目录只读保留，不复用；旧 V30—V33 不重建。

## 4. QA 工具就绪门禁

1. 在获批根启动并拒绝未登记、越界、reparse、身份变化和非法子进程。
2. SQLite sidecar 退役竞态只在主库和 sidecar 已登记、当前路径与父目录严格复核通过时核对；其他情况失败关闭。
3. 历史归档与当前增量登记均可机器读取，无需复制历史台账。
4. 子进程、缓存、环境变量和 patch 在成功及异常路径均恢复。
5. Python/Godot 命令清单与授权一致，报告脱敏。
6. QA 自测、ruff format/check 和 mypy 通过。

S1 固定验证顺序为：V1 三个 QA 文件 AST/入口/选择器静态核对；V2 Ruff check 与 format --check；V3 strict mypy；V4 fixture 与调用参数矩阵；V5 一次 `--tool-readiness --root <当前获批精确根>`；V6 Git、进程、8000/8001 端口、环境、临时路径、旧证据与容量收尾。V1—V4 失败不得创建运行根；V5 失败保留根并进入剩余额度判断；不得自动转入完整 quality。

冻结须同时满足：V1—V6 全通过；外层与受保护 pytest exit 0；failed/skipped/xfailed 均为 0 且 not_run 为空；首错没有被后续错误覆盖；进程、端口、环境与临时路径恢复；报告脱敏；旧证据和历史台账逐项不变；没有未知 Git 变化；且 `quality_started=false`、`performance_started=false`、`product_service_started=false`。满足后只请求一次 S2 完整 quality 授权，`step6_complete` 仍保持 false。

2026-09-18 quality09在完整backend pytest暴露QA阶段根契约缺陷：前八命令通过，pytest为1877 passed、133 skipped、1 failed。失败测试将活动quality根传给只接受S1_ROOTS的readiness授权器。用户随后显式批准最小修复、tool15/R6双上下文验证和最终quality10。修复不得扩大readiness根授权，只能修正测试的阶段语义并补足消费方覆盖。

本合同最小修复与V1–V4已通过，tool15随后137函数/532参数全部通过；R6的17选择器/42节点也全部passed，但native observer drain超时使R6整体失败、当前readiness回执不能签发。修复/tool15/R6均1/1已用。quality10前置未满足，根未创建且禁止执行；不得重跑R6或进入S3。

用户再次确认所有必要授权均已预先通过，现建立R7限定合同：只移除 `NativeWatcher.drain()` 等待marker期间的重复完整check，保持10秒、observer错误传播及marker确认后的完整ledger/容量/identity检查；新增确定性慢check回归，并将活动根轮换到tool16/R7。修复/tool16/R7各1次，任一步失败停止；全部通过后才恢复quality10条件授权。

R7最小修复与V1–V4已通过：tool16固定138函数/533参数，R7固定18选择器/43节点，Ruff、strict mypy、矩阵/新回归2 passed及43节点collect均通过；冻结指纹见evidence。修复1/1已用，tool16/R7仍0/1且根未创建；下一项只运行一次tool16。

tool16唯一运行已通过：138函数/533参数全passed，父observer完成且无overflow、unknown、reparse或边界违规，环境与端口恢复。tool16 1/1已用；R7仍0/1且根未创建，下一项只运行一次R7。

R7唯一运行已通过：18选择器/43节点全部完成且无failed/skipped/xfailed/not_run，正式readiness回执有效，native observer及所有恢复项通过。实际恢复区950,898,725 bytes，加quality10 1GiB上限仍≤2GiB；QA工具冻结，quality10条件授权现生效且仅可运行一次。

最终quality10唯一运行已通过：九阶段全部exit0；完整pytest 2,942 passed、133既有契约skip、0 failed。133项均属于未配置历史诊断根的既有四文件分布53/45/33/2，与quality09一致；未新增skip、未改语义。native observer完整且资源恢复，quality10 1/1已用，禁止任何后续完整quality批次。S3固定性能现已到达并按用户总授权执行一次。

机械准备修正最多两轮。有证据支持的 QA 缺陷最多两轮修复验证；第二次同类失败前须共同根因复盘。不得隐藏真实越界、身份变化或未授权资源。

## 5. 完整语义验收

tool15与R6双上下文就绪后冻结代码、QA 工具、环境、用例集合和资源配置；当前只执行一次 `native-quality-10`。它保持原quality语义，包含repository preflight/final、lock、ruff、mypy、schema/contracts、Godot import/unit、connectivity、dialogue connectivity和完整backend pytest；相关精确回环、25-case evaluator、三进程digest、安全、预算投影、成本归属、重试/熔断、SQLite/WAL/FULL、空间、故障和对抗回归均以实际完整集合结果为准。

所有 skip 须有当前契约依据。不得新增 skip、吞异常或删除慢样本。任一正式验收失败立即停止，不再开新 quality 批次。

历史 quality-13 只有在同一冻结版本的完整门禁通过、竞态回归通过、边界反例仍拒绝且无新增未知错误时，才能充分关闭。单次选择性通过不能关闭。

## 6. 性能与空间契约

只在语义全通过后执行一次固定协议：统一 warm-up 一次，随后 5 个测量 run；不得追加批次。沿用当前阈值，不恢复历史 30 ms / 2 ms，也不放宽。

| 场景 | 门禁 |
| --- | --- |
| control SQLite 增长 | ≤ 256 KiB |
| observability SQLite 增长 | ≤ 512 KiB |
| 合计 SQLite 增长 | ≤ 768 KiB |
| 无 recorder | p95 ≤ 50 ms；p99 ≤ 100 ms |
| 内存 recorder | p95 ≤ 3.5 ms；p99 ≤ 5 ms；配对增量 ≤ 0.75 ms；吞吐 ≥ off 的 80% |
| SQLite observability | p95 ≤ 150 ms；p99 ≤ 200 ms；吞吐 ≥ 8 req/s |
| pre-dispatch reject | p95 ≤ 50 ms；dispatch = 0；control 增长 ≤ 256 KiB |
| 完整本地回环 on | p95 ≤ 250 ms；p99 ≤ 400 ms；吞吐 ≥ 4 req/s；control 增长 ≤ 256 KiB |
| on/off 回环差值 | p95 增量 ≤ 140 ms |
| 三 SQLite 诊断 | 单库延迟仅诊断；须满足各自及合计空间上限和 dispatch ownership |

分别报告延迟、吞吐、空间余量与正确性。性能输出根只在 QA 作用域内绑定并恢复，不修改 runner 或阈值。

实际S3唯一矩阵完成1次warm-up+5次测量，8场景 `failure_codes=[]`、`warning_codes=[]`。无recorder、内存、SQLite observability、pre-dispatch reject及完整回环的延迟、吞吐、空间和dispatch ownership全部满足上表；on/off p95增量约117.3461ms≤140ms，内存p95增量约0.0837ms≤0.75ms。三SQLite p95/p99只产生合同预定义的非阻断诊断，空间及ownership通过。S3 1/1已用，禁止追加运行或修改阈值。

## 7. 完成与停止

文档治理、工具门禁、完整quality、原失败关闭和性能门禁现已全部通过，正式状态写入 `step6_complete=true` 和 `awaiting_step_7_authorization=true`。下一项只能由用户单独授权Step 7交付收口。

遇到产品缺陷、需要 Schema/产品 runner 修改、需放宽安全或阈值、未知 Git 变化、资源越界或额度耗尽时停止，不进入 Step 7，不执行 Git 交付。
