# 方法论同步说明

## 用途与范围

2026-09-05，按用户要求接入 `E:\Vibe coding` 更新后的方法论。本文件只记录本次接入差异，不替代当前任务卡、计划或验收证据，也不建立新的产品任务。

| 位置 | 本次变化 |
| --- | --- |
| 正式 `AGENTS.md` | 接入方法论入口、授权规则与权威文档路径 |
| 正式 `README.md`、`docs/README.md` | 移除过期“无活动任务”断言，改为引用当前任务；明确 worktree 权威关系 |
| 正式 `docs/testing-strategy.md` | 接入测试分类、QA 就绪与性能指标规则；区分历史结果与当前验收 |
| 功能 worktree `AGENTS.md` | 引导读取正式规则；保留既有安全边界 |

原正式五份项目管理文档及其历史本轮不改写，压缩治理留待原开发对话执行。业务代码、测试、migration、runner、阈值与既有资源均不属于本次修改范围。

## 恢复执行时的基线说明

- 核对时正式分支为 `main`，F-009 为 `feat/f-009-safety-cost-performance`；二者 HEAD 均为 `1a4fc2cdf142b00f823bebf9b2abe6e74ba23ab3`。此为本次本地快照，不代表新 fetch 的远端状态。
- 同步前功能 worktree 有 98 个变更文件（37 tracked modified、61 untracked）；本次新增一项 tracked `AGENTS.md` 变更，现为 99 项。已逐项核对原 98 个文件的 SHA-256，全部不变；恢复时仍须重新核验，不把文件数量本身当作完整性证明。
- 正式目录原有五份未提交项目管理文档继续保留，本次另修改四份入口/策略文件并新增本说明；旧“正式仅五文档修改”的预检条件已不再适用。
- 历史验收结论只证明对应历史冻结版本；新授权必须审查当前实际 diff 与新基线，不能自动批准未知变更或将新文档指纹冒充原验收指纹。
- 当前产品阶段以正式任务卡为准。接入时已核对：`canonical-diagnostic-01` 仅一次目标尝试通过，原故障 `original_failure_resolved=false`、`step6_complete=false`；本说明不重新宣布或修改该结论。

## 验证与授权

两处 `git diff --check` 通过，原 98 项功能变更文件 SHA-256 全部不变；本次文档引用与新增说明单独核查。不运行产品、QA 或性能测试。没有提交、推送、PR、合并、网络访问或资源删除。原五份历史文档的 CRLF 提示属于既有换行状态，本轮未格式化这些文件。

有限机械修正、资源批次与本地提交均需当前任务的明确授权。本次仅同步规则，不将可选授权追溯应用到 F-009；用户在开发对话中提交的后续 Prompt 才决定恢复范围。
