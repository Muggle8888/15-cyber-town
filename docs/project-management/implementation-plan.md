# 当前实施计划：F-011 NPC 回访体验强化

更新时间：2026-09-22

状态：`GIT_DELIVERY_IN_PROGRESS`

## Step 0：UI 契约与首个正常状态

- 冻结三名 NPC 的普通话题草稿、长期主题值和四类确定性状态写入。
- 在现有 640×360 底部对话框上方增加紧凑“话题与记忆”弹层。
- 正常状态仅展示普通话题、记住主题、询问记忆、简洁回复、自然回复和忘记主题。
- 生成 Godot 实机截图。用户确认首个正常状态后，才扩展确认、请求锁定、失败和重试视觉状态。

## Step 1：引导记忆闭环

- 普通话题和记忆询问只填入输入框。
- 记住、忘记与回复风格先显示 NPC、动作和具体内容，再由玩家确认。
- 客户端分别冻结原始载荷、自然中文展示文本和动作类型；手动重试复用同一请求 ID 与完全相同的载荷，不重复历史。
- 请求期间禁用弹层、关闭、NPC 切换和重复提交；三个 NPC 的菜单、历史和可重试状态相互隔离。

## Step 2：关系与回复风格上下文

- 保持公开 API 与 Schema 不变，在内部 Provider 请求加入经过枚举校验的可选关系阶段和回复风格。
- Provider dispatch 前读取当前 NPC 的关系阶段与持续回复风格，生成 `f-011-relationship-style-v1` 受控系统说明。
- 关系读取失败时省略该上下文并继续普通对话；不得伪造阶段。
- 记住、忘记与风格设置继续是零 Provider、零关系变化的本地确定性操作。
- 关系状态在客户端明确区分获取中、已验证和暂不可用；空历史开场白只使用已验证阶段。

## Step 3：自动与 Fake 验证

- 覆盖菜单映射、填入与确认门禁、原始载荷/展示文本分离、精确重试和跨 NPC 隔离。
- 覆盖 4 个关系阶段、2 个回复风格、读取失败降级、公开 Schema 不变与 Persona/安全优先级。
- 覆盖 SQLite 双端重启持久化、主题匹配召回、忘记后的确定性降级和零 Provider 状态写入。
- 运行相关 Godot、Python 与 Fake 回环，再执行统一质量入口；不调用真实模型。

## Step 4：用户试玩与真实模型 UAT

- 用户分别验收三名 NPC 的记住、离开、双端重启、返回、召回、忘记与关系语气体验。
- 真实模型 UAT 已按单独授权执行：计划 6 次全部完成，未使用额外失败重试，SDK 自动重试为 0，实际费用 USD 0.001039。
- UAT 使用隔离 F-011 数据库、玩家标识和验收账本；记录仅包含检查结果、调用数、token、费用和时间戳。

## Step 5：文档与交付

- 分别记录代码/自动验证、Fake、用户试玩与真实模型验收。
- 三层验收和质量门禁全部通过后才建议完成。
- commit、push、PR、CI、合并与任务归档已获用户授权；资源处置、强推、分支删除、tag 和发布不在授权范围。
