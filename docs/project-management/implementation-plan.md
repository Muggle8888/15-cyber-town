# F-014 暮光信号余波：实施计划

更新时间：2026-09-22

状态：`READY_FOR_GIT_DELIVERY`

## Step 0：故事与视觉门禁

- 冻结三种等价方案、六次事件对话草稿、失败提示和结局文案。
- 复用当前 640×360 画面实现一个静态三方案选择状态。
- 生成真实 Godot 截图 `godot-aftermath-choice-v1.png`。
- 用户确认位置、大小、文字和信息层级前，不进入状态机、存档或完整交互。

完成结果：用户已批准真实 Godot 三方案选择画面，视觉门禁通过。

## Step 1：状态与事件协调

- 新增独立 F-014 状态控制器和小型事件协调器。
- 实现严格存档校验、F-013 前置解锁、跨重启恢复和确认重置。
- F-013 重温期间暂停展示 F-014，但不得删除或回退 F-014。

完成结果：独立版本化状态、严格组合校验、损坏恢复和两事件协调均已实现并通过定向测试。

## Step 2：咨询与确定性选择

- 接入 Nia 开场、Ivo/Rhea 任意顺序咨询、可见草稿和精确请求快照。
- 三方意见齐全后开放信号校准台选择面板和二次确认。
- 选择操作零 Provider、零长期记忆、零直接关系变化，并立即保存与更新画面。

完成结果：可见可编辑草稿、请求快照、二次确认与三种确定性方案均已实现；Fake 回环证明选择不增加 Provider 调用。

## Step 3：三人回访与视觉余波

- 三名 NPC 可任意顺序回访，只有正确 `completed` 回复计数。
- 为三种结局实现固定灯光、标牌、事件卡和完成总结。
- 完成状态提供只重置 F-014 的“重温余波”。

完成结果：三人任意顺序回访、三种灯光与标牌、完成总结及独立重温均已实现；结局截图已生成。

## Step 4：三层验收

- 自动/Fake 覆盖解锁、任意顺序、选择、错误状态、精确重试、跨重启、损坏恢复、F-013 协调和既有回归。
- 用户使用隔离存档完成选择、重启、三人回访和重温试玩。
- 真实模型 UAT 另行授权：计划 6 次，最多 2 次语义补测，硬上限 8 次，SDK 自动重试 0，费用硬上限 USD 0.05。
- 全部通过后另行申请 commit、push、PR、CI、合并与归档授权。

当前结果：自动/Fake 与统一质量通过。用户关闭 Fake 演示并明确跳过选择恢复、三人回访、完成和重温验证，这些项目未记为人工通过。真实模型 UAT 已通过：6 次调用完成 6 项检查，0 次语义补测，1807 输入 token、807 输出 token、1513 micro-USD、未决 0。Git 交付授权已按“UAT 通过后生效”的条件生效。
